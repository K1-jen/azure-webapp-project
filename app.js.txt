const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.get('/', (req, res) => {
    res.send('🚀 My Azure App is Live with Database Connected!');
});

app.listen(port, () => {
    console.log(`App running on port ${port}`);
});